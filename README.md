# ストレッチタイマー (Stretch Timer)

🧘 ストレッチメニューを選んでタイマーで実行できるアプリ

## 機能

1. **ストレッチメニュー選択** - プリセットから好きなストレッチを選択
2. **部位別メニュー** - 肩、首、腰、脚、腕、背中、全身
3. **秒数カスタマイズ** - 30秒〜3分の範囲で設定可能
4. **音声ガイド** - 残り時間を読み上げ
5. **振動通知** - 残り時間で振動でお知らせ
6. **連続実行** - 複数のストレッチを順番に実行
7. **ルーティン保存** - よく使う組み合わせを保存

## 技術スタック

- Expo SDK 52
- Expo Router 4
- TypeScript
- AsyncStorage (データ永続化)
- expo-speech (音声読み上げ)
- expo-haptics (振動フィードバック)
- Jest + React Native Testing Library (テスト)

## 開発

```bash
# 依存関係のインストール
npm install

# 開発サーバー起動
npm start

# テスト実行
npm test

# カバレッジ付きテスト
npm test -- --coverage
```

## プロジェクト構成

```
stretch-timer/
├── app/              # Expo Router screens
│   ├── _layout.tsx   # Root layout
│   ├── index.tsx     # Home screen
│   ├── select.tsx    # Stretch selection
│   ├── session.tsx   # Timer session
│   ├── settings.tsx  # Settings
│   └── routines.tsx  # Saved routines
├── components/       # Reusable components
├── hooks/            # Custom hooks
├── types/            # TypeScript types
├── utils/            # Utility functions
└── __tests__/        # Test files
```

## ライセンス

MIT
